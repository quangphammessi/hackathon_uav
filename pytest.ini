[pytest]
testpaths = tests
pythonpath = . libs/agentmarket_core services/storefront
markers =
    integration: needs real PostgreSQL/Redis; skipped automatically when absent
filterwarnings =
    ignore::DeprecationWarning
